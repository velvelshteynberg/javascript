scrollby scrolls by a certain amount 
scrollto scrolls to a certain place
scrollIntoView() goes to the thing that its called on
element.scrollIntoView({behavior: 'smooth'})
location object in js .href
.replace
.assign

There is something called history too
history.back()



navigator

Date() creates a date object

Error object is new Error('Somethinig went wront');
you can throw this error whenever you want